<UserControl x:Class="TIAS.Elements.ElementUnit"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             MouseDown="Unit_Click"
             Width="55" Height="55"
             Unloaded="UserControl_Unloaded">

    <UserControl.Resources>
        <Style x:Key="UnitBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                    <Setter Property="BorderBrush" Value="Gold"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="Gold" BlurRadius="10" ShadowDepth="0"/>
                        </Setter.Value>
                    </Setter>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Border Style="{StaticResource UnitBorderStyle}" CornerRadius="5">
        <Grid>
            <Image x:Name="UnitImage" 
                   Source="/Images/Unit/T34.png" 
                   Width="45" Height="45"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   RenderOptions.BitmapScalingMode="HighQuality"/>

            <!-- Полоска здоровья -->
            <Border Background="DarkRed" 
                    Height="5" 
                    VerticalAlignment="Bottom" 
                    HorizontalAlignment="Center"
                    Width="40"
                    Margin="0,0,0,2"
                    CornerRadius="2">
                <Border x:Name="HealtBar" 
                        Background="Green"
                        HorizontalAlignment="Left"
                        Width="40"
                        CornerRadius="2"/>
            </Border>

            <!-- Индикатор действий (можно ли ходить/атаковать) -->
            <Border x:Name="ActionIndicator"
                    Width="10" Height="10"
                    CornerRadius="5"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="2"
                    Background="Green"
                    Visibility="Collapsed"/>
        </Grid>
    </Border>
</UserControl>